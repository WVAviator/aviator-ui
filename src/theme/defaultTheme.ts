import { Theme } from "@emotion/react";

const defaultTheme: Theme = {
	colors: {
		primary: "#00bcd4",
		secondary: "#ff9800",
		tertiary: "#f44336",
		background: "#fafafa",
		foreground: "#212121",
		textLight: "#ffffff",
		textDark: "#000000",
	},
};

export default defaultTheme;
